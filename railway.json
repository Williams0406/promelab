{
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "rootDirectory": "main",
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn main.wsgi"
  }
}
